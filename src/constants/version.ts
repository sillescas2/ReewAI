/**
 * ReewAI Application Version
 * 
 * Reglas de versionado:
 * - Inicio: V1.0.17
 * - El último dígito (patch) se incrementa de forma secuencial con cada interacción / iteración.
 * - El primer (major) y segundo (minor) dígito se modifican únicamente de forma manual.
 */
export const APP_VERSION = 'V1.0.20';
